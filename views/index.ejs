<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <h1>Health-Hub</h1>
  <h4>Appoinment information made ridiculously simple</h4>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } %>
  <p>Welcome! View your visit summary by using the navigation bar above.</p>
</main>

<%- include('./partials/footer') %>
